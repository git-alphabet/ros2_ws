
# CLAUDE.md


## 项目开发与调试规范

1. **第一次回答最后发1,第二次最后发2,以此类推。**
2. **禁止盲目猜测。** 可以推断但必须告知用户，对某些方面不清楚就问用户，并查阅历史记录。
3. **日志分析：** 如果贴了日志，先阐述日志内容和含义。
4. **开发环境：** Ubuntu 22.04，ROS2 Humble，开发在 Docker 容器内，宿主机代码挂载到容器，代码直接在宿主机改，日志看容器内输出。
5. **文件阅读：** 看完用户给的文件再操作。
6. **代码修改：** 不要写无用内容，添加代码时要看完整份文件。
7. **仿真命名空间：** 仿真时 node/topic/action 等都加 namespace 前缀。查看 tf tree 用：
	```bash
	ros2 run rqt_tf_tree rqt_tf_tree --ros-args -r /tf:=tf -r /tf_static:=tf_static -r __ns:=/red_standard_robot1
	```
	实车不带 ns，如 `/cmd_vel`。
8. **最佳实践：** 遵循最佳实践，充分利用 VS Code 插件，并说明插件用法。
9. **调试流程：** 复现 → 定位 → 排查 → 解决 → 验证 → 复盘。
10. **目标：** 可以自动迭代。
11. **思考方式：** 用英文思考，用中文回答。

---

## 参考

详细开发、构建、运行、Docker 说明见 README.md。
