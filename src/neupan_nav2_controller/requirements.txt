# NeuPAN (neupan_nav2_controller) Python runtime dependencies
#
# Notes:
# - Pin NumPy < 2 to avoid ABI issues with some wheels (e.g. older PyTorch builds).
# - SciPy >= 1.15 introduces sparse "*_array" types (e.g. csc_array) that can break
#   older solver wrappers expecting sparse matrices. Keeping SciPy < 1.15 avoids this.
# - If you install the CPU-only PyTorch wheel, you may need the PyTorch CPU index, e.g.:
#     python3 -m pip install --extra-index-url https://download.pytorch.org/whl/cpu -r requirements.txt

numpy<2
scipy<1.15

PyYAML==6.0.3

torch==2.1.0+cpu

# NeuPAN vendor code imports these directly (e.g. blocks/dune_train.py)
colorama==0.4.6
rich==13.9.4

cvxpy==1.7.5
cvxpylayers==0.1.9
diffcp==1.1.4
ecos==2.0.14

# CVXPY solver backends (CVXPY depends on these; listing explicitly improves reproducibility)
clarabel==0.11.1
osqp==1.0.5
scs==3.2.9
