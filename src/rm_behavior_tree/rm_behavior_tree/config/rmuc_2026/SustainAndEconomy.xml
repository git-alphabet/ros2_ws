<?xml version="1.0" encoding="UTF-8"?>
<root BTCPP_format="4">

    <BehaviorTree ID="SustainAndEconomy">
        <ReactiveFallback>
            <!-- Heal when low (prefer supply; allow remote only when disengaged & safe) -->
            <SubTree ID="HealPlan" _autoremap="true"/>
            <!-- Ammo management (prefer supply free ticks & station purchase; remote only when disengaged) -->
            <SubTree ID="AmmoPlan" _autoremap="true"/>
        </ReactiveFallback>
    </BehaviorTree>

</root>
